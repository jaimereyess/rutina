const TittleDay = ({ title }) => {
    return (
        <>
            <h1 className="mt-2 text-3xl font-bold underline text-center">{title}</h1>
        </>
    );
};

export default TittleDay;
