import Nav from "./navbar";

const LeterDays = ({ day }) => {
    return (
        <>
            <h1 className="mt-2 text-3xl font-bold underline text-center">{day}</h1>
        </>
    );
};

export default LeterDays;
